import CommonButton from "../components/common/CommonButton";

const Login = () => {
  return (
    <div className="w-full p-5">
      <CommonButton
        label="카카오로 시작하기"
        bgColor="bg-[#FAE100]"
        textColor="text-text"
      />
    </div>
  );
};

export default Login;
